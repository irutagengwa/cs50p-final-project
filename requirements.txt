# Runtime dependencies only. Do not `pip freeze` into this file — that pulls in
# CS50 tooling (submit50/lib50 -> cryptography, lxml) which has to build from
# source on the deploy host and fails there.
Flask>=3.1
gunicorn>=23.0
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
